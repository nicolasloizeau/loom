"""loom: images of woven cloth from a JSON settings file.

A drawdown generator (loom.generators) gives the pattern; loom.plain turns it
into a flat image, loom.blender into a rendered one, finished by loom.post
(film grain and background). loom.settings reads the JSON and fills in the
defaults. See README.md.
"""
